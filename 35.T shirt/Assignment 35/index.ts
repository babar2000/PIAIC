function make_shirt(size: string, message: string) {
  console.log(`Creating a ${size} T-shirt with the message: "${message}"`);
}

// Call the function to create a T-shirt
make_shirt("Large", "I love coding!");

// Call the function again with different arguments
make_shirt("Medium", "Stay curious!");
